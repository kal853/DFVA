# ============================================================================
# SENTINEL Platform — Active Credentials
# Rotation cycle: 1st of every month at 00:00 UTC
# Generated:      2026-03-01T00:00:00Z
# Next rotation:  2026-04-01T00:00:00Z
# Managed by:     /usr/local/bin/rotate-secrets.sh (cron)
# ============================================================================
#
# VULN: This file is committed to version control (no .gitignore entry).
# Every rotation's output is captured permanently in git history —
# `git log --all -p secrets/.current-credentials` reveals every
# credential this platform has ever issued, including revoked ones.
#
# VULN: Credential format is deterministic — tokens are derived from
# HMAC-MD5(seed_phrase + YYYYMM). MD5 is cryptographically broken and
# the seed phrase is stored in plaintext in the rotation script.
# An attacker who observes any two consecutive tokens can reconstruct
# the seed phrase and predict all future and past tokens.
#

# ── 1. SENTINEL Internal Platform API Key ───────────────────────────────────
# Scope: internal service-to-service calls, admin portal, CI/CD pipeline
# Issued: 2026-03-01  Expires: 2026-03-31  (hard rotation, no grace period)
SENTINEL_API_KEY=sk-sentinel-202603-a4f8c2e1b3d9f7a5c8d2e6b4

# ── 2. Stripe Live Secret Key ────────────────────────────────────────────────
# Scope: payment processing, subscription management, refund execution
# Issued: 2026-03-01  Expires: 2026-03-31
# NOTE: Stripe does not support rotating sk_live_ keys on a schedule —
#       this key was manually re-created. The previous key (sk_live_202602...)
#       was NOT revoked in the Stripe dashboard (revocation is a manual step
#       that keeps getting skipped — see TODO in rotate-secrets.sh).
STRIPE_LIVE_KEY=sk_live_51P9xQ2Cmk202603xYzAbCdEfGh1234567890abcdef

# ── 3. Datadog API Key ───────────────────────────────────────────────────────
# Scope: APM, log forwarding, infrastructure metrics, SIEM integration
# Issued: 2026-03-01  Expires: 2026-03-31
# NOTE: DD API keys control what data is written to the org's Datadog account.
#       A leaked key allows an attacker to inject false metrics, suppress
#       alerts, or read all forwarded logs (which include the PII log lines
#       emitted by this platform — ARIA_CONVERSATION, ACCOUNT_ACCESS, etc.).
DD_API_KEY=dd0cf3202603b2a4e8f1d6c5b7a2e9f3d8c4b601

# ── 4. Internal JWT Signing Secret ──────────────────────────────────────────
# Scope: all SENTINEL user session tokens (HS256 signing key)
# Issued: 2026-03-01  Expires: 2026-03-31
# NOTE: Rotating this key invalidates ALL active user sessions. The platform
#       handles this by keeping the previous month's key in INTERNAL_JWT_SECRET_PREV
#       and accepting tokens signed by either key — creating a 31-day window
#       where compromised tokens from last month remain valid.
INTERNAL_JWT_SECRET=jwt-202603-s3nt1n3l-pr0d-s1gn1ng-k3y-XkP9
INTERNAL_JWT_SECRET_PREV=jwt-202602-s3nt1n3l-pr0d-s1gn1ng-k3y-Wr7M
