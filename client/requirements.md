## Packages
framer-motion | Smooth animations and hacker-like transitions
lucide-react | High-quality icons for the toolkit interface
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Space Grotesk", "sans-serif"],
  mono: ["JetBrains Mono", "monospace"],
}
Colors to add in tailwind config or use via CSS variables:
Brand colors with a hacker aesthetic (neon greens, deep slates, alert reds).
